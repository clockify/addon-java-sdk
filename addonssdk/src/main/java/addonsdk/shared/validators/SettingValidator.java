package addonsdk.shared.validators;

import addonsdk.shared.model.Settings;

public class SettingValidator implements Validator<Settings> {
    @Override
    public void validate(Settings obj) {
    }
}
